/** Central constants for assistant generative UI layout. */

export const ASSISTANT_PANEL_MIN_WIDTH = 360;

/** Stable id for workflow step_list blocks (live SSE + persisted metadata). */
export const ASSISTANT_WORKFLOW_STEP_LIST_BLOCK_ID = 'workflow-steps';
