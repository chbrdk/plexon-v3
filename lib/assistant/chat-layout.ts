import type { SxProps, Theme } from '@mui/material';

/** Message list stack — full width bubble column (AUDION glass panel rhythm). */
export const assistantChatMessagesStackSx: SxProps<Theme> = {
  p: { xs: 1, md: 2.5 },
  pb: { md: '100px' },
  gap: 3,
  width: '100%',
  maxWidth: '100%',
};
