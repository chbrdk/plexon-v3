/** EQC routes that wait on CHECKION GEO (dual layer) + domain scan.
 * Next.js `export const maxDuration` MUST be a numeric literal (not this import). */
export const EQC_LONG_RUNNING_MAX_DURATION_SEC = 900;
