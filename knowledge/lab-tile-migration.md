# LabTile React cutover (local)

**Date:** 2026-09-14

PLEXON Event Quick Check domain magazine uses `@msqdx/ui` `LabTile` / `LabTileStrip`. Iterate locally against sibling `msqdx-ui` before Coolify pin bumps.

See also CHECKION `knowledge/lab-tile-migration.md`.
