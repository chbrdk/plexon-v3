# Magazine lede cutover (2026-08-03)

Uses `@msqdx/ui` — see `msqdx-ui/knowledge/components/lede-filter-panel.md`.

- Admin overview stats: `Lede` + `LedeStrip`
- Barrel exports `Lede`/`LedeStrip` (aliases `StatLede*` remain)
